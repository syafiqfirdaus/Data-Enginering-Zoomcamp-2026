# stub asset for ingestion.trips

def run(connection, taxi_types=None):
    # placeholder: would read raw taxi data and write to a table
    print("Running trips ingestion with types:", taxi_types)


# tests would be defined in pipeline.yml via quality checks
